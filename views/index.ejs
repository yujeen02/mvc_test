<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>유저 목록</title>
    <link rel="stylesheet" href="/static/css/index.css" />
  </head>
  <body>
    <h1>유저 목록</h1>
    <!-- 🛑 팝업 창 -->
    <% if (showPopUp) { %>
    <!-- 🛑 팝업 창 -->
    <div id="popup" class="popup">
      <div class="popup-content">
        <h2>공지사항</h2>
        <p>이 사이트는 테스트 중입니다. 이용해 주셔서 감사합니다!</p>
        <button onclick="closePopup()">닫기</button>
        <button onclick="closePopupForToday()">오늘 하루 보지 않기</button>
      </div>
    </div>
    <% } %>
    <!-- 📝 유저 추가 폼 -->
    <form id="userForm">
      <input type="text" id="username" placeholder="유저명 입력" required />
      <input
        type="password"
        id="password"
        placeholder="비밀번호 입력"
        required
      />
      <button type="submit">추가</button>
    </form>

    <!-- 📋 유저 목록 테이블 -->
    <table border="1">
      <thead>
        <tr>
          <th>ID</th>
          <th>유저명</th>
          <th>비밀번호</th>
          <th>수정</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(user => { %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.username %></td>
          <td><%= user.password %></td>
          <td><button onclick="editUser(`<%= user.id %>`)">수정</button></td>
          <td><button onclick="deleteUser(`<%= user.id %>`)">삭제</button></td>
        </tr>
        <% }); %>
      </tbody>
    </table>

    <script src="/static/js/index.js"></script>
  </body>
</html>
